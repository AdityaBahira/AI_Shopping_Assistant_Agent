rules:
  - id: detect-google-api-key
    pattern-regex: 'AIzaSy[A-Za-z0-9_\-]*'
    message: "Security warning: Hardcoded Google API key prefix detected. Avoid hardcoding API keys in source files. Use environment variables or a secrets manager instead."
    languages:
      - python
    severity: ERROR
